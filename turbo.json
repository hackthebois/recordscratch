{
	"$schema": "https://turbo.build/schema.json",
	"tasks": {
		"build": {
			"outputs": ["dist/**"]
		},
		"typecheck": {},
		"dev": {
			"persistent": true,
			"cache": false
		},
		"clean": {}
	},
	"globalEnv": ["DATABASE_URL"]
}
